<div class="sauna-list-section text-white py-5">
  <div class="container bg-dark bg-opacity-50 rounded-4 p-4 shadow-lg">

    <h1 class="text-center mb-4 fw-bold">サウナ一覧</h1>

    <!-- 🔍 検索フォーム -->
    <div class="search-container mb-4">
      <%= form_with url: saunas_path, method: :get, local: true do %>

        <div class="row g-3 align-items-stretch">

          <!-- 都道府県 -->
          <div class="col-md-3 d-flex">
            <%= select_tag :prefecture,
                  options_for_select(
                    Sauna.prefectures.map { |key, _| [Sauna.new(prefecture: key).prefecture_name, key] },
                    params[:prefecture]
                  ),
                  include_blank: "すべて",
                  class: "form-select flex-fill" %>
          </div>

          <!-- サウナ名 -->
          <div class="col-md-5 d-flex">
            <%= text_field_tag :keyword, params[:keyword],
                  placeholder: "サウナ名で検索",
                  class: "form-control flex-fill" %>
          </div>

          <!-- 検索ボタン -->
          <div class="col-md-2 d-flex">
            <%= submit_tag "検索", class: "btn btn-primary w-100" %>
          </div>

        </div>

      <% end %>
    </div>

    <hr class="border-light">

    <% if @saunas.any? %>
       <ul class="list-unstyled">
         <% @saunas.each do |sauna| %>
           <li class="glass-item">
             <%= link_to sauna.name, sauna_path(sauna) %>
             <span class="glass-badge"><%= sauna.prefecture_name %></span>
           </li>
         <% end %>
       </ul>
    <% else %>
       <p class="mt-3 text-center text-light">
         該当するサウナは見つかりませんでした。
        </p>
    <% end %>
  </div>
</div>
